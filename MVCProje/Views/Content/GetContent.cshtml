@using Entities.Concrete
@model List<Content>

@{
    ViewBag.Title = "GetContent";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@using (Html.BeginForm("GetContent","Content",FormMethod.Post))
            {
                <p>
                    Aranacak Kelimeyi Giriniz : @Html.TextBox("content")
                    <input type="submit" value="Ara"/>
                </p>
            }
<h2>GetContent</h2>

    @foreach (var x in Model)
    {
               <p>@x.ContentValue</p> 
        }

